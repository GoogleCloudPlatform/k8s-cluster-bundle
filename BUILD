# Run with bazel run //:gazelle
# gazelle:proto disable_global
load("@bazel_gazelle//:def.bzl", "gazelle")

gazelle(
    name = "gazelle",
    prefix = "github.com/GoogleCloudPlatform/k8s-cluster-bundle",
)
