apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  creationTimestamp: null
  labels:
    controller-tools.k8s.io: "1.0"
  name: packagedeployments.bundle.gke.io
spec:
  group: bundle.gke.io
  names:
    kind: PackageDeployment
    plural: packagedeployments
  scope: Namespaced
  validation:
    openAPIV3Schema:
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          properties:
            componentSets:
              items:
                properties:
                  setName:
                    description: SetName is the readable name of a component set.
                    type: string
                  version:
                    description: Version is the version string for a component set.
                    type: string
                type: object
              type: array
            version:
              type: string
          type: object
        status:
          properties:
            components:
              description: ComponentName is the readable name of a component.
              items:
                properties:
                  componentName:
                    type: string
                  status:
                    type: string
                  statusMessage:
                    type: string
                  version:
                    type: string
                type: object
              type: array
          type: object
  version: v1alpha1
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
